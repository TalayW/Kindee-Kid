# Prompt สำหรับใช้กับ Codex

คัดลอกข้อความด้านล่างไปใช้เมื่ออยากให้ Codex ช่วยพัฒนาแอปต่อ

```text
โปรเจกต์นี้เป็นเว็บแอปภาษาไทยชื่อ “กินดี Kids” สำหรับนักเรียนประถม ป.4–ป.6
ใช้ HTML, CSS และ JavaScript แบบไม่ใช้ framework และไม่ใช้ backend

กติกาสำคัญ:
1. รักษาการทำงานแบบ offline-first และการเก็บข้อมูลด้วย localStorage
2. ห้ามเพิ่มระบบลดน้ำหนัก เป้าหมายน้ำหนัก การจำกัดแคลอรี หรือข้อความที่ทำให้เด็กรู้สึกผิดเรื่องอาหาร
3. แสดงชัดเจนว่าค่าหมู่อาหารเป็นค่าประมาณเพื่อการเรียนรู้
4. ทุกปุ่มต้องใช้ได้บนมือถือและมี label ที่เหมาะกับ accessibility
5. ห้ามใส่ API key ลงในโค้ดฝั่ง browser
6. ต้องไม่อ้างว่าใช้ AI หากยังไม่มีโมเดลทำงานจริง
7. หลังแก้ไข ให้ตรวจ JavaScript syntax และทดสอบการบันทึก แก้ไข ลบ ดูกราฟ ส่งออก CSV และนำเข้า JSON

โครงสร้าง:
- index.html: หน้าจอและข้อความ
- styles.css: responsive design
- app.js: data model, localStorage, scoring, chart, export/import
- manifest.webmanifest และ sw.js: PWA/offline

งานที่ต้องการให้ทำต่อ:
[เขียนสิ่งที่ต้องการตรงนี้ เช่น “เพิ่มหน้าแบบสอบถามความพึงพอใจ 5 ข้อ และส่งออกผลเป็น CSV”]
```
